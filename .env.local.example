# .env.local.example — Template de variáveis de ambiente (desenvolvimento)

# --- SUPABASE (cliente público) ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=public-anon-key-here

# --- SUPABASE (server-only) ---
# Chave com privilégios (service role). MANTENHA SOMENTE NO SERVIDOR/CI
SUPABASE_SERVICE_ROLE_KEY=service-role-key-keep-secret

# URL pública do app
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_VERSION=1.0.0

# --- Impersonation / Cookies ---
# Nome do cookie httpOnly usado para Visualizar como Usuário
# Use o valor padrão `impersonate_user_id` para compatibilidade com o código
IMPERSONATE_COOKIE_NAME=impersonate_user_id
IMPERSONATE_COOKIE_TTL=7200
# Em produção, use COOKIE_SECURE=true
COOKIE_SECURE=false
COOKIE_SAMESITE=Lax

# --- Inngest / Workers ---
# Chaves usadas por webhooks e jobs (server-only)
INNGEST_API_KEY=dev_inngest_key
INNGEST_SIGNING_SECRET=dev_inngest_signing_secret
INNGEST_EVENT_KEY=local_development

# --- Outras integrações (exemplos de dev) ---
ASAAS_API_KEY=asaas_dev_key
SENDGRID_API_KEY=sendgrid_dev_key

# --- Configurações operacionais ---
NODE_ENV=development
LOG_LEVEL=debug

# --- Uso ---
# 1. Copie este arquivo para .env.local
#    cp .env.local.example .env.local
# 2. Ajuste os valores sensíveis (não comitar .env.local)
# 3. Rode 'pnpm run check-env' para validar antes de 'pnpm dev' ou 'pnpm build'
